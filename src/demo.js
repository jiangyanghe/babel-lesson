const fn = () => 1; // 语法

const name = 'Y.H.JIANG';
const hi = `hi ${name}`;
const b = [1,2,3];

b.includes(1); // api
const c = Object.assign(a, b);

for(let i of b) {
  console.log(i);
}

Promise.resolve().finally();